<h1> Workouts </h1>

<ul>All Workouts</ul>
<% if !@workouts.nil? %>
  <% @workouts.each do |workout| %>
    <li><%= link_to workout.title, workout_path(workout) %> <%= workout.workout_type %></li>
  <% end %>
  </ul>
<% end %>
